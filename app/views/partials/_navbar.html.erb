<nav class="navbar navbar-default" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="speak-box">
      <a class="speak-logo" href="/">Speak</a>
    </div>

    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapser">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="navbar-collapser">
      <ul class="nav navbar-nav">

        <% if current_user %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle logged-in" data-toggle="dropdown"><%=current_user.username%><span class="caret"></span></a>
          <ul class="dropdown-menu" role="menu">
            <li><%= link_to "Profile", user_path(current_user.id) %></li>
            <li><a href="#">Account</a></li>
            <li class="divider"></li>
            <li><%= link_to "Log Out", logout_path, method: "delete" %></li>
          </ul>
          <% else %>
          <li><a data-toggle="modal" href="#signUpModal">Sign Up</a></li>
          <li><a data-toggle="modal" href="#loginModal">Login</a></li>
          <% end %>

        </ul>
      </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
  </nav>

  <!-- MODALS -->

  <div class="modal fade " id="signUpModal" tabindex="-1" role="dialog" aria-hidden="true">
    <%= render partial: "modals/signup"%>
  </div>

  <div class="modal fade" id="loginModal" tabindex="-1" role="dialog" aria-hidden="true">
    <%= render partial: "modals/login"%>
  </div>
